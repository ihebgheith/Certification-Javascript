📘 FICHE MÉMO JAVASCRIPT (COMPLÈTE) :

🔹 Variables
var → portée fonction (ancien, à éviter)
let → portée bloc, modifiable
const → portée bloc, non réassignable (mais objet mutable)

🔹 Types primitifs
number → nombres (1, 3.14)
string → texte ("hello")
boolean → true / false
undefined → variable non définie
null → absence volontaire de valeur
symbol → identifiant unique
bigint → grands entiers

👉 Tout le reste est un objet

🔹 Opérateurs
=== → égalité stricte (type + valeur)
!== → différence stricte
?? → nullish coalescing
?. → optional chaining
&& / || → logique
... → spread / rest

🔹 Conditions
if / else
switch
condition ? valeur1 : valeur2

🔹 Boucles
for
while
do...while
for...of → valeurs (tableaux, strings)
for...in → clés (objets)

🔹 Fonctions
function nom() {} → classique
const fn = () => {} → fléchée
return → valeur retournée
paramètres par défaut
rest parameters (...args)

🔹 Tableaux (Array)
    Transformation
map → transformer
filter → filtrer
reduce → accumuler
flat / flatMap → aplatir

    Recherche
find / findIndex

some / every
includes
indexOf / lastIndexOf
at

    Modification (⚠️ mutables)
push / pop
shift / unshift
splice
sort / reverse
fill
copyWithin

    Lecture
slice
concat
join
keys / values / entries
forEach

🔹 Objets (Object)
    Lecture
Object.keys
Object.values
Object.entries
Object.hasOwn

    Copie
{ ...obj }
Object.assign
structuredClone

    Sécurité
Object.freeze
Object.seal
Object.preventExtensions
Object.isFrozen / isSealed / isExtensible

    Propriétés
defineProperty
getOwnPropertyDescriptor
getOwnPropertyNames

    Prototype
Object.create
Object.getPrototypeOf

🔹 JSON

JSON.stringify → objet → JSON
JSON.parse → JSON → objet

🔹 Dates
new Date()
Date.now()
getFullYear / getMonth / getDate
toLocaleDateString

🔹 Math
Math.random
Math.floor / ceil / round
Math.max / min
Math.pow / sqrt

🔹 Strings
length
toUpperCase / toLowerCase
includes
slice / substring
split
replace / replaceAll
trim

🔹 Promises & Async
Promise.then / catch / finally
async / await
Promise.all
Promise.race

🔹 Erreurs
try {
} catch (e) {
} finally {
}

🔹 DOM (navigateur)
document.querySelector
addEventListener
classList.add/remove/toggle
innerHTML / textContent
style

🔹 Console (debug)
console.log
console.error
console.warn
console.table
console.group
console.time / timeEnd

🔹 Bonnes pratiques
Toujours ===
Éviter mutations involontaires
Préférer fonctions pures
Nommer clairement variables & fonctions
Utiliser map/filter/reduce

